# Copy to .env.local for local dev, and set the SAME two vars in Vercel
# (Project → Settings → Environment Variables) before sharing the URL.
#
# These are the shared username + password the whole team uses to log in.
BASIC_AUTH_USER=team
BASIC_AUTH_PASSWORD=shakira2026
